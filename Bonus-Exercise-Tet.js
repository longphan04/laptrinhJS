const x = 100;
const result = (x < 1000) ? "nhỏ hơn 1000" : "lớn hơn 1000";

const variable2 = variable1 || "";

let x, y, z = 3;

if (isTurnOn);

for (let item in sampleArr)

for (let i = 0; i < 100000; i++){}

const x = 1, y = 2;
const obj = {x, y}

sayHello = name => console.log('Hello', name);
setTimeout(() => console.log('Loaded'), 2000);
List.forEach(item => console.log(item));

getValue = ratio => ratio * 6.9;

getValue = (a,b = 3, c = 4) => (a * b + c);

const welcome = `You have logged in as ${first} ${last}.`;

const lorem = `Lorem ipsum dolor sit amet, consectetur
        adipisicing elit, sed do eiusmod tempor incididunt
        ut labore magna aliqua. Ut enim ad minim 
        veniam, quis notrud exercitation ullammo laboris
        nisit ut aliquip ex ea commodo consequat. Duis aute
        irure dolor in reprehenderit in voluptate velit esse.`



import {action, service} from 'lib';
const {form, errrors, entity, controller, component} = this.props;

const odd = [1, 3, 5];
const nums = [2, ...odd, 4, 6];

mandatory = () => {throw new Error('Thiếu tham số! '); }
sample = (param1 = mandatory()) => param1

const name = 'Emp A'
emp = employees.first(item => item.name === name);

console.log(validate({firstName: 'Duy', LastName: 'Buffet'}));


console.log(validate(rule, {firstName: 'Duy'}));
console.log(validate(rule, {firstName: 'Duy', LastName: 'Buffet'}));

~~6.9 === 6